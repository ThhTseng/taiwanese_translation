# Uncomment the next line to define a global platform for your project
 platform :ios, '10.0'

target 'taiwanese_translation' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!
  #pod 'Alamofire', '~> 4.7'
  # Pods for taiwanese_translation
  pod 'SwiftSocket'  


  target 'taiwanese_translationTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'taiwanese_translationUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
